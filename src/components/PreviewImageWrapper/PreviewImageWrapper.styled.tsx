import styled from 'styled-components';

export const StyledPreviewImageWrapper = styled.div`
   width: 100%;
   cursor: pointer;

   img {
      display: block;
      width: 100%;
      height: auto;
      border-radius: 15px;
   }
`;
