import { StyledCart } from './Cart.styled';

const Cart = () => {
   return (
      <StyledCart>
         <h2>Cart</h2>

         <div>
            <p>Your cart is empty.</p>
         </div>
      </StyledCart>
   );
};
export default Cart;
